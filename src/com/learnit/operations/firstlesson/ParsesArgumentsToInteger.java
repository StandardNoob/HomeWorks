package com.learnit.operations.firstlesson;

public class ParsesArgumentsToInteger {

	public static void main(String[] args) {
		int firstArgs = Integer.parseInt(args[0]);
		int secondArgs = Integer.parseInt(args[1]);
		System.out.println(firstArgs + secondArgs);
		// Run as -> Run configurations -> add 2 arguments
	}

}

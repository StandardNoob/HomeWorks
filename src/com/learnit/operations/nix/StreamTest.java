package com.learnit.operations.nix;

import java.util.Arrays;
import java.util.stream.Collectors;

import com.learnit.operations.bookservice.Book;

public class StreamTest {
//	Arrays.stream(books).filter(book -> book.getPublishingYear() == yearFromInclusively).collect(Collectors.toList());
//	Arrays.stream(books).filter(book -> book.getPublisher().equals(publisher)).toArray(Book[]::new);

}
